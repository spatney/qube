{
  "name": "qube",
  "version": "2.3.2",
  "description": "Simple implementation of an in-memory analytics cube. ",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "deploy": "rm -rf ./dist && tsc && npm publish",
    "test": "rm -rf ./dist && npm run build && node ./examples/example.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/spatney/qube"
  },
  "author": "Sachin Patney",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.6.3"
  }
}
